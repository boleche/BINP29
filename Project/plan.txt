




1. Parse ClinVar for SNPs.
2. Parse through bed, bim, and fam AADR files to retrieve matching rsIDs from clinVar data.
    - output a tsv that indicates individuals with SNP matches
    - pay attention to recessive and dominant (maybe output if you are a carrier)
3. Script which will parse though individual SNP info and output a tsv with clinvar matches
4. Script which will tell you which overlapping matches an individual has with ancient DNA clinvar markers
5. develop an interface where an individual can upload their own info and recieve these tables
    - make these tables interactive 
    - map??
    - plotly 


prometheus website 
clinvar confidence (convert confidence?)
divide diseases by group 

1. ref vs. alt (0 - no snp, 1 - hetero, 2 - homo alt (look at dom vs. recessive vs. neither))

should i output whether on y or mt ??



needs:

- provide sample submission files to view X
- output a spread of all disease variants and where in the body they effect
- explorer of all the 31 SNPs found in ancient people X
- output info about the ancient individual you share a SNP with
- aesthetics 
- add a drop na line of code to clinvar user parsing